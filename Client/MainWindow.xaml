<Window x:Class="Client.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Messenger Client" Height="600" Width="800"
        Background="#1E1E1E" Foreground="White">

	<Window.Resources>
		<Style TargetType="Button">
			<Setter Property="Background" Value="#007ACC"/>
			<Setter Property="Foreground" Value="White"/>
			<Setter Property="BorderThickness" Value="0"/>
			<Setter Property="Padding" Value="10,5"/>
			<Setter Property="Margin" Value="5"/>
			<Setter Property="FontWeight" Value="Bold"/>
		</Style>

		<Style TargetType="TextBox">
			<Setter Property="Background" Value="#2D2D30"/>
			<Setter Property="Foreground" Value="White"/>
			<Setter Property="BorderBrush" Value="#3E3E42"/>
			<Setter Property="BorderThickness" Value="1"/>
			<Setter Property="Padding" Value="5"/>
		</Style>

		<Style TargetType="TextBlock">
			<Setter Property="Foreground" Value="#CCCCCC"/>
			<Setter Property="VerticalAlignment" Value="Center"/>
		</Style>
	</Window.Resources>

	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="*"/>
			<RowDefinition Height="Auto"/>
		</Grid.RowDefinitions>

		<!-- Header -->
		<Border Grid.Row="0" Background="#007ACC" Padding="15">
			<StackPanel Orientation="Horizontal">
				<TextBlock Text="💬 " FontSize="20"/>
				<TextBlock Text="Messenger Client" FontSize="16" FontWeight="Bold"/>
			</StackPanel>
		</Border>

		<!-- Connection -->
		<StackPanel Grid.Row="1" Orientation="Horizontal" Margin="10">
			<TextBlock Text="IP:" VerticalAlignment="Center" Margin="0,0,5,0"/>
			<TextBox x:Name="txtServerIp" Width="100" Text="127.0.0.1" Margin="0,0,10,0"/>
			<TextBlock Text="Port:" VerticalAlignment="Center" Margin="0,0,5,0"/>
			<TextBox x:Name="txtServerPort" Width="60" Text="8888" Margin="0,0,10,0"/>
			<TextBlock Text="Name:" VerticalAlignment="Center" Margin="0,0,5,0"/>
			<TextBox x:Name="txtUsername" Width="100" Text="User" Margin="0,0,10,0"/>
			<Button x:Name="btnConnect" Content="Connect" Click="BtnConnect_Click" Width="80"/>
			<Button x:Name="btnDisconnect" Content="Disconnect" Click="BtnDisconnect_Click" Width="80" Margin="10,0,0,0" IsEnabled="False"/>
		</StackPanel>

		<!-- Chat -->
		<Border Grid.Row="2" Margin="10" Background="#0F0F0F" CornerRadius="5">
			<TextBox x:Name="txtChat" IsReadOnly="True" Background="Transparent"
                     Foreground="White" BorderThickness="0" FontSize="14"
                     VerticalScrollBarVisibility="Auto"/>
		</Border>

		<!-- Message Input -->
		<StackPanel Grid.Row="3" Orientation="Horizontal" Margin="10">
			<TextBox x:Name="txtMessage" Width="400" IsEnabled="False"
                     KeyDown="TxtMessage_KeyDown"/>
			<Button x:Name="btnSend" Content="Send" Click="BtnSend_Click"
                    Width="80" Margin="10,0,0,0" IsEnabled="False"/>
		</StackPanel>
	</Grid>
</Window>